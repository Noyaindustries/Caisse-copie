# Profil kiosk offline (chargé avec `--mode offline`)
# Force le fallback local pour toutes les images produits.
VITE_DISABLE_REMOTE_IMAGES=1

# Pas de sync cloud en mode offline.
# Laisser vide pour rester en local.
VITE_CLOUD_SYNC_URL=
