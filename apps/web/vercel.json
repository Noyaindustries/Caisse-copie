{
  "framework": "nextjs",
  "installCommand": "cd ../.. && npm ci",
  "buildCommand": "cd ../.. && node scripts/vercel-build.mjs",
  "rewrites": [
    {
      "source": "/health",
      "destination": "/api/health"
    }
  ]
}
