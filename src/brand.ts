/** Marque affichée dans l’interface (vitrine, barre latérale, connexion). */
export const BRAND_LOGO_SRC = '/branding/logo-circle.svg' as const
/** Logo complet (PNG) pour favicon / OG / usages hors chrome UI. */
export const BRAND_LOGO_FULL_SRC = '/branding/caisse-ci-logo.png' as const
export const BRAND_NAME = 'Caisse CI' as const
