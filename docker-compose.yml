services:
  mongo:
    image: mongo:7
    container_name: caisseci-mongo
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - caisseci-mongo-data:/data/db

volumes:
  caisseci-mongo-data:
